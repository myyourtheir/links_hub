import { View, Text } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

const AddingScreen = () => {
	return (
		<SafeAreaView>
			<Text>AddingScreen</Text>
		</SafeAreaView>
	)
}

export default AddingScreen