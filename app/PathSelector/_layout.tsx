import { View, Text } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

const PathSelectorLayout = () => {
	return (
		<SafeAreaView>
			<Text>PathSelectorLayout</Text>
		</SafeAreaView>
	)
}

export default PathSelectorLayout